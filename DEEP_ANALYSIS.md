# 🔍 Глубокий анализ изменений в core_rg

**Дата анализа:** 2024  
**Версия:** После удаления Kafka и внедрения полной системы авторизации

---

## 📊 Статистика изменений

### Файлы
- **Новых файлов:** ~20
- **Измененных файлов:** ~15
- **Удаленных файлов:** 1 (`kafka.publisher.ts`)
- **Новых миграций:** 1 (`CreateInitialAdmin1735123456789.ts`)

### Зависимости
- **Добавлено:** `jsonwebtoken`, `@types/jsonwebtoken`
- **Удалено:** `kafkajs`

### Модули
- **Новый модуль:** `presentation/authorization/` (полная система авторизации)
- **Новый guard:** `GatewayAuthGuard` (глобальный)
- **Новые guards:** `RolesGuard`, `SelfOrRolesGuard`, `BaseAuthGuard`
- **Новые сервисы:** `JwtExtractorService`, `AuthorizationService`

---

## 🏗️ Архитектурные изменения

### 1. Многоуровневая система безопасности

#### Уровень 1: Network Security (GatewayAuthGuard)
**Файл:** `src/presentation/guards/gateway-auth.guard.ts`

**Назначение:** Защита от внешних запросов, не прошедших через gateway

**Механизм:**
- Проверка shared secret (`GATEWAY_AUTH_TOKEN`)
- Поддержка нескольких заголовков: `x-gateway-auth`, `integrationAuthToken`, `integrationauthtoken`
- Глобальный guard (зарегистрирован как `APP_GUARD`)
- Декоратор `@Public()` для исключения endpoints

**Статус:** ✅ Реализовано и работает

**Особенности:**
- Graceful degradation: если токен не настроен, выбрасывает ошибку
- Логирование всех попыток доступа
- Поддержка публичных endpoints через `@Public()`

#### Уровень 2: Authentication (JWT Extraction)
**Файл:** `src/presentation/authorization/services/jwt-extractor.service.ts`

**Назначение:** Извлечение и декодирование JWT из запросов

**Механизм:**
- Извлечение из `Authorization: Bearer <token>`
- Опциональная верификация JWT (через `VERIFY_JWT_IN_CORE=true`)
- Декодирование без верификации по умолчанию (gateway уже проверил)
- Валидация ролей (фильтрация невалидных ролей)
- Кеширование пользователя в `request[REQUEST_USER_KEY]`

**Статус:** ✅ Реализовано

**Особенности:**
- Поддержка `id` и `sub` полей из JWT
- Валидация ролей против `Role` enum
- Логирование невалидных ролей
- Graceful handling отсутствующих токенов

#### Уровень 3: Authorization (Role-based Access Control)
**Файлы:**
- `src/presentation/authorization/guards/roles.guard.ts`
- `src/presentation/authorization/guards/self-or-roles.guard.ts`
- `src/presentation/authorization/services/authorization.service.ts`

**Назначение:** Проверка прав доступа на основе ролей

**Механизм:**
- `RolesGuard` - проверка наличия требуемых ролей
- `SelfOrRolesGuard` - разрешение self-update или требование ролей
- `BaseAuthGuard` - базовая логика извлечения пользователя
- Декоратор `@RequireRoles(...)` для указания требуемых ролей
- Декоратор `@CurrentUser()` для инъекции пользователя в контроллеры

**Статус:** ✅ Реализовано и интегрировано

**Особенности:**
- Кеширование пользователя в request (оптимизация)
- Логирование всех решений авторизации
- Defense-in-depth: проверки на уровне guard и handler

---

### 2. Структура модуля авторизации

```
presentation/authorization/
├── roles.enum.ts                          # Enum всех ролей платформы
├── interfaces/
│   └── request-user.interface.ts          # RequestUser { id, roles[] }
├── services/
│   ├── jwt-extractor.service.ts           # Извлечение JWT из запросов
│   └── authorization.service.ts           # Централизованная логика авторизации
├── guards/
│   ├── base-auth.guard.ts                 # Базовый guard с общей логикой
│   ├── roles.guard.ts                     # Проверка ролей
│   └── self-or-roles.guard.ts             # Self-update или роли
├── decorators/
│   ├── require-roles.decorator.ts         # @RequireRoles(...)
│   └── current-user.decorator.ts          # @CurrentUser()
├── constants/
│   └── privileged-roles.constant.ts        # PRIVILEGED_ROLES
└── index.ts                               # Public API модуля
```

**Принципы:**
- Separation of Concerns: каждый компонент отвечает за свою задачу
- DRY: общая логика в `BaseAuthGuard`
- Type Safety: строгая типизация через TypeScript
- Extensibility: легко добавить новые guards/decorators

---

### 3. Интеграция авторизации в контроллеры

#### CommandController
```typescript
@Controller('accounts')
@UseGuards(RolesGuard)  // Глобальный guard для контроллера
export class CommandController {
  @Post()
  @RequireRoles(Role.ROLE_PLATFORM_ACCOUNT_RW, Role.ROLE_PLATFORM_ADMIN)
  async create(@Body() dto: CreateAccountDto, @CurrentUser() user: RequestUser) {
    // user.id и user.roles передаются в команду
  }

  @Patch(':id')
  @RequireRoles(Role.ROLE_PLATFORM_ACCOUNT_RW, Role.ROLE_PLATFORM_ADMIN)
  @UseGuards(SelfOrRolesGuard)  // Специальный guard для self-update
  async update(@Param('id') id: string, @Body() dto: UpdateAccountDto, @CurrentUser() user: RequestUser) {
    // Self-update разрешен, но с ограничениями в handler
  }

  @Post('password/reset')
  @Public()  // Публичный endpoint
  async resetPassword(@Body() dto: ResetAccountPasswordDto) {
    // Не требует JWT, только gateway auth
  }
}
```

**Особенности:**
- Все endpoints защищены `RolesGuard` на уровне контроллера
- Специфичные роли через `@RequireRoles`
- Публичные endpoints через `@Public()`
- `SelfOrRolesGuard` для гибкой логики self-update

#### QueryController
```typescript
@Controller('accounts')
@UseGuards(RolesGuard)
export class QueryController {
  @Get(':id')
  async getOne(@Param('id') id: string, @CurrentUser() user: RequestUser) {
    // Проверка прав в handler (owner или ROLE_PLATFORM_ACCOUNT_RO)
  }

  @Get()
  @RequireRoles(Role.ROLE_PLATFORM_ACCOUNT_RO, Role.ROLE_PLATFORM_ADMIN)
  async getAccounts(@Query() query: GetAccountsDto) {
    // Требует роль для списка
  }
}
```

---

### 4. Интеграция авторизации в handlers

#### Command Handlers

**CreateAccountHandler:**
- ✅ Проверка привилегированных ролей (только `ROLE_PLATFORM_ADMIN`)
- ✅ Обязательный `requesterId` и `requesterRoles`
- ✅ Логирование попыток создания с привилегированными ролями

**UpdateAccountHandler:**
- ✅ Проверка self-update (владелец не может менять email/roles)
- ✅ Проверка привилегированных ролей
- ✅ Правильное сравнение массивов ролей (через `Set`)
- ✅ Defense-in-depth проверки

**DeleteAccountHandler:**
- ✅ Запрет на удаление себя
- ✅ Обязательный `requesterId` и `requesterRoles`
- ✅ Defense-in-depth проверки

#### Query Handlers

**GetUserByIdHandler:**
- ✅ Опциональный `requesterId` (для гибкости)
- ✅ Проверка прав: owner или `ROLE_PLATFORM_ACCOUNT_RO`
- ✅ Логирование решений авторизации

**GetAccountsHandler:**
- ✅ Опциональный `requesterId`
- ✅ Базовая проверка в guard, детальная в handler

---

### 5. Удаление Kafka

**Что удалено:**
- `KafkaPublisher` класс
- Зависимость `kafkajs`
- Kafka и Zookeeper из `docker-compose.yml`
- Публикация событий в Kafka из `EventStoreService`

**Что осталось:**
- `EventEmitter2` для внутренних проекций
- Event Store в PostgreSQL
- Read Model в Redis
- `UserProjection` через `EventEmitter2`

**Влияние:**
- ✅ Внутренняя логика не изменилась
- ✅ Проекции работают через `EventEmitter2`
- ⚠️ События больше не публикуются для других сервисов (но никто не потреблял)

---

## 🔐 Безопасность

### 1. Защита от внешних запросов

**Механизм:** `GatewayAuthGuard` (глобальный)

**Проверки:**
- Наличие заголовка с gateway auth token
- Совпадение токена с `GATEWAY_AUTH_TOKEN`
- Логирование всех попыток доступа

**Уязвимости:**
- ⚠️ Если gateway скомпрометирован, можно подделать запросы
- ✅ Митигация: gateway должен быть защищен на уровне сети

### 2. JWT Authentication

**Механизм:** `JwtExtractorService`

**Проверки:**
- Извлечение JWT из `Authorization` header
- Опциональная верификация (если `VERIFY_JWT_IN_CORE=true`)
- Валидация ролей

**Уязвимости:**
- ⚠️ По умолчанию JWT не верифицируется (только декодируется)
- ✅ Митигация: gateway верифицирует JWT перед отправкой
- ⚠️ Если gateway скомпрометирован, можно подделать JWT
- ✅ Митигация: включить `VERIFY_JWT_IN_CORE=true` в production

**Рекомендации:**
- Включить `VERIFY_JWT_IN_CORE=true` в production
- Использовать `JWT_PUBLIC_KEY` для верификации

### 3. Role-based Authorization

**Механизм:** Guards + Handlers

**Проверки:**
- Наличие требуемых ролей
- Права владельца (self-access)
- Привилегированные роли (только `ROLE_PLATFORM_ADMIN`)
- Ограничения для self-update

**Уязвимости:**
- ✅ Defense-in-depth: проверки на уровне guard и handler
- ✅ Логирование всех решений
- ✅ Централизованная константа `PRIVILEGED_ROLES`

### 4. Публичные endpoints

**Endpoints:**
- `POST /accounts/password/reset` - сброс пароля
- `POST /accounts/password/change` - изменение пароля
- `POST /internal/access-check` - проверка доступа

**Защита:**
- ✅ Требуют только `GatewayAuthGuard`
- ✅ Не требуют JWT
- ✅ Помечены `@Public()`

**Риски:**
- ⚠️ Brute force на password reset (нет rate limiting)
- ✅ Митигация: gateway должен иметь rate limiting

---

## 📋 Соответствие с оригинальным core

### 1. API Endpoints

| Endpoint | Core (Symfony) | Core_rg | Соответствие |
|----------|----------------|---------|--------------|
| `GET /accounts` | ✅ | ✅ | ✅ |
| `GET /accounts/{id}` | ✅ | ✅ | ✅ |
| `GET /accounts/email/{email}` | ❌ | ✅ | ⚠️ Новый |
| `POST /accounts` | ✅ | ✅ | ✅ |
| `PATCH /accounts/{id}` | ✅ | ✅ | ✅ |
| `DELETE /accounts/{id}` | ✅ | ✅ | ✅ |
| `POST /accounts/password/reset` | ✅ | ✅ | ✅ |
| `POST /accounts/password/change` | ✅ | ✅ | ✅ |
| `PATCH /accounts/{id}/approve` | ❌ | ✅ | ⚠️ Новый (не используется) |
| `PATCH /accounts/{id}/block` | ❌ | ✅ | ⚠️ Новый (не используется) |
| `PATCH /accounts/{id}/role` | ❌ | ✅ | ⚠️ Новый (не используется) |

### 2. Авторизация

| Действие | Core (AccountVoter) | Core_rg | Соответствие |
|----------|---------------------|---------|--------------|
| CREATE | `ROLE_PLATFORM_ACCOUNT_RW` | `ROLE_PLATFORM_ACCOUNT_RW` или `ROLE_PLATFORM_ADMIN` | ✅ |
| READ | `ROLE_PLATFORM_ACCOUNT_RO` или owner | `ROLE_PLATFORM_ACCOUNT_RO` или owner | ✅ |
| UPDATE | `ROLE_PLATFORM_ACCOUNT_RW` или owner (с ограничениями) | `ROLE_PLATFORM_ACCOUNT_RW` или owner (с ограничениями) | ✅ |
| DELETE | `ROLE_PLATFORM_ACCOUNT_RW` и не себя | `ROLE_PLATFORM_ACCOUNT_RW` и не себя | ✅ |
| LIST | `ROLE_PLATFORM_ACCOUNT_RO` | `ROLE_PLATFORM_ACCOUNT_RO` | ✅ |

**Дополнительные проверки в core_rg:**
- ✅ Проверка привилегированных ролей (только `ROLE_PLATFORM_ADMIN`)
- ✅ Более строгая проверка self-update (сравнение через `Set`)

### 3. Форматы данных

**DTO:**
- ✅ `CreateAccountDto` - совместим
- ✅ `UpdateAccountDto` - совместим
- ✅ `GetAccountsDto` - совместим

**Ответы:**
- ✅ Hydra формат (`application/ld+json`)
- ✅ `HydraMember` для одного объекта
- ✅ `HydraCollection` для списка

---

## 🔄 Соответствие с gateway

### 1. Проксирование запросов

**Gateway → Core_rg:**
- ✅ Передает JWT в `Authorization: Bearer <token>`
- ✅ Передает gateway auth token в `integrationAuthToken` header
- ✅ Проксирует все основные endpoints

**Проблемы:**
- ⚠️ Gateway не проксирует новые endpoints:
  - `PATCH /accounts/{id}/approve`
  - `PATCH /accounts/{id}/block`
  - `PATCH /accounts/{id}/role`
  - `GET /accounts/email/{email}`

**Влияние:**
- Эти endpoints не будут работать через gateway
- Но они не используются в adminpanel, так что это не критично

### 2. Аутентификация

**Gateway проверяет JWT:**
- ✅ Верифицирует JWT перед отправкой в core_rg
- ✅ Извлекает пользователя из JWT
- ✅ Передает JWT в `Authorization` header

**Core_rg:**
- ✅ Извлекает JWT из `Authorization` header
- ✅ Декодирует (или верифицирует, если включено)
- ✅ Извлекает `id` и `roles` из JWT

**Совместимость:** ✅

---

## 🖥️ Соответствие с adminpanel

### 1. Используемые endpoints

**Adminpanel использует:**
- `GET /api/core/accounts` - список
- `GET /api/core/accounts/{id}` - один
- `POST /api/core/accounts` - создать
- `PATCH /api/core/accounts/{id}` - обновить
- `DELETE /api/core/accounts/{id}` - удалить
- `POST /api/core/accounts/password/reset` - сброс пароля
- `POST /api/core/accounts/password/change` - изменение пароля

**Соответствие:** ✅ Все endpoints поддерживаются

### 2. Форматы данных

**Adminpanel ожидает:**
- Hydra формат для старого API (`Users.getList()`)
- Простой формат для нового API (`CoreAccountsService`)

**Core_rg возвращает:**
- Hydra формат для всех endpoints

**Gateway преобразует:**
- Hydra → простой формат для `CoreAccountsService`

**Совместимость:** ✅

### 3. Проблемы

**Ошибка типизации в adminpanel:**
- ⚠️ `roles: Array<any[]>` вместо `roles: string[]` в сгенерированных типах
- **Решение:** Перегенерировать OpenAPI спецификацию

---

## ⚠️ Потенциальные проблемы

### 1. Критичные

**Нет:**
- ❌ Rate limiting на gateway (защита от brute force)
- ❌ JWT верификация по умолчанию (только декодирование)

**Риски:**
- Brute force на password reset
- Подделка JWT, если gateway скомпрометирован

**Митигация:**
- Gateway должен иметь rate limiting
- Включить `VERIFY_JWT_IN_CORE=true` в production

### 2. Важные

**Отсутствующие endpoints в gateway:**
- ⚠️ `PATCH /accounts/{id}/approve`
- ⚠️ `PATCH /accounts/{id}/block`
- ⚠️ `PATCH /accounts/{id}/role`
- ⚠️ `GET /accounts/email/{email}`

**Влияние:**
- Эти endpoints не будут работать через gateway
- Но они не используются, так что это не критично

**Ошибка типизации в adminpanel:**
- ⚠️ `roles: Array<any[]>` вместо `roles: string[]`
- **Решение:** Перегенерировать OpenAPI спецификацию

### 3. Средние

**Self-update проверка:**
- ⚠️ В core_rg нет доступа к "оригинальным" данным до обновления
- Проверка выполняется по текущим данным из Read Model
- **Риск:** Если Read Model не актуален, проверка может быть неточной
- **Митигация:** Read Model обновляется синхронно через `EventEmitter2`

**Зависимость от JWT:**
- ⚠️ Если JWT отсутствует или невалиден, запросы не пройдут
- **Митигация:** Gateway должен всегда передавать валидный JWT

---

## ✅ Что работает хорошо

### 1. Архитектура

- ✅ Многоуровневая защита (Gateway Auth + JWT + Roles)
- ✅ Separation of Concerns (guards, services, handlers)
- ✅ DRY принцип (BaseAuthGuard)
- ✅ Type Safety (строгая типизация)
- ✅ Логирование всех решений авторизации

### 2. Безопасность

- ✅ Защита от внешних запросов (GatewayAuthGuard)
- ✅ Role-based access control
- ✅ Проверка привилегированных ролей
- ✅ Ограничения для self-update
- ✅ Defense-in-depth (проверки на нескольких уровнях)

### 3. Соответствие

- ✅ API совместим с core
- ✅ Авторизация соответствует AccountVoter
- ✅ Форматы данных совместимы
- ✅ Gateway корректно проксирует запросы

### 4. Производительность

- ✅ Кеширование пользователя в request
- ✅ Оптимизация извлечения JWT (один раз на запрос)
- ✅ EventEmitter2 для внутренних проекций (быстрее Kafka)

---

## 🔧 Рекомендации

### 1. Немедленные действия

1. **Включить JWT верификацию в production:**
   ```env
   VERIFY_JWT_IN_CORE=true
   JWT_PUBLIC_KEY=<public_key>
   ```

2. **Добавить rate limiting на gateway:**
   - Защита от brute force на password reset
   - Защита от DDoS

3. **Перегенерировать OpenAPI спецификацию для adminpanel:**
   - Исправить ошибку типизации `roles`

### 2. Улучшения

1. **Добавить endpoints в gateway (если понадобятся):**
   - `PATCH /accounts/{id}/approve`
   - `PATCH /accounts/{id}/block`
   - `PATCH /accounts/{id}/role`
   - `GET /accounts/email/{email}`

2. **Добавить валидацию Content-Type:**
   - Проверка `application/merge-patch+json` для PATCH

3. **Улучшить логирование:**
   - Структурированные логи
   - Метрики авторизации

### 3. Документация

1. **Описать различия между core и core_rg**
2. **Документировать новые endpoints**
3. **Создать migration guide для adminpanel**

---

## 📈 Метрики качества

| Метрика | Оценка | Комментарий |
|---------|--------|-------------|
| **Безопасность** | 9/10 | Многоуровневая защита, но нет rate limiting |
| **Соответствие с core** | 9/10 | Логика идентична, но реализация разная |
| **Соответствие с gateway** | 8/10 | Отсутствуют некоторые endpoints |
| **Соответствие с adminpanel** | 8/10 | Ошибка типизации в сгенерированных типах |
| **Архитектура** | 9/10 | Чистая, расширяемая, хорошо структурированная |
| **Производительность** | 9/10 | Оптимизации (кеширование, EventEmitter2) |
| **Поддерживаемость** | 9/10 | Хорошая структура, логирование, типизация |

**Общая оценка:** 8.7/10

---

## 🎯 Выводы

### Положительные моменты

1. ✅ **Полная система авторизации** - соответствует требованиям
2. ✅ **Многоуровневая защита** - Gateway Auth + JWT + Roles
3. ✅ **Соответствие с core** - логика авторизации идентична
4. ✅ **Хорошая архитектура** - чистая, расширяемая, типизированная
5. ✅ **Удаление Kafka** - упрощение без потери функциональности
6. ✅ **Логирование** - все решения авторизации логируются

### Проблемы

1. ⚠️ **Отсутствие rate limiting** - риск brute force
2. ⚠️ **JWT не верифицируется по умолчанию** - риск подделки
3. ⚠️ **Отсутствующие endpoints в gateway** - но не критично
4. ⚠️ **Ошибка типизации в adminpanel** - нужно перегенерировать

### Итог

**Core_rg готов к использованию** с небольшими доработками:
- Включить JWT верификацию в production
- Добавить rate limiting на gateway
- Перегенерировать OpenAPI спецификацию

**Архитектура:** ✅ Отличная  
**Безопасность:** ✅ Хорошая (с рекомендациями)  
**Соответствие:** ✅ Высокое (8.7/10)

---

## 📝 Чеклист перед production

- [ ] Включить `VERIFY_JWT_IN_CORE=true`
- [ ] Настроить `JWT_PUBLIC_KEY`
- [ ] Добавить rate limiting на gateway
- [ ] Перегенерировать OpenAPI спецификацию для adminpanel
- [ ] Протестировать все endpoints через gateway
- [ ] Настроить мониторинг авторизации
- [ ] Документировать различия с core
- [ ] Создать migration guide

---

**Анализ выполнен:** ✅  
**Статус:** Готов к использованию с рекомендациями

