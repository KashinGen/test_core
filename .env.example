# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=core_db

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Application
PORT=3000
NODE_ENV=development
GATEWAY_AUTH_TOKEN=super-secret-token

# JWT Configuration
# JWT_PUBLIC_KEY - публичный ключ для верификации JWT (обязательно!)
# Формат: b64:<base64_encoded_key> (такой же, как и в gateway CORE_JWT_PUBLIC_KEY)
JWT_PUBLIC_KEY=

# CORS Configuration
CORS_WHITELIST=http://localhost:3000